name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # services: では起動コマンドを渡せないため docker run を使う (entrypoint/command
      # キーは検証で拒否され、options: の --entrypoint では引数を渡せない)。
      - name: Start Firestore emulator
        id: firestore
        background: true
        run: |
          docker run --rm -p 8080:8080 \
            --entrypoint sh gcr.io/google.com/cloudsdktool/google-cloud-cli:emulators \
            -c "gcloud emulators firestore start --host-port=0.0.0.0:8080"

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: "26"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm run lint

      - name: Typecheck
        run: pnpm run typecheck

      - name: Build
        run: pnpm run build

      - name: Wait for Firestore emulator
        run: timeout 60 bash -c 'until curl -sf -o /dev/null http://localhost:8080/; do sleep 1; done'

      - name: Test
        run: pnpm test
        env:
          FIRESTORE_EMULATOR_HOST: localhost:8080

      - name: Stop Firestore emulator
        cancel: firestore
