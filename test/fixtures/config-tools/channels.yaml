channels:
  - channel: "default"
    trigger:
      when:
        - kind: mention
  - channel: "C0000000TOOLS"
    session:
      mode: channel
      idleResetMinutes: 30
    reply:
      mode: flat
    agent:
      tools: [read, grep]
      excludeTools: [write, edit]
